# Инструкция по заполнению операций для производства изделий из баннеров

## Введение

Этот документ описывает:
1. **Как заполнять операции в форме заказа** (фронтенд, CreateOrderForm)
2. **Как настраивать шаблоны операций в админке** (Admin Panel → Materials)

Операции настраиваются в **админке** для каждого материала отдельно. Ниже — полные инструкции по созданию и использованию операций для баннеров.

---

## Админка: Добавление операций к материалу

### Доступ к админке
1. Перейдите по маршруту `/admin` (требуются права администратора)
2. Выберите вкладку **"Materials"**
3. Кликните на материал (например, "Баннер 1000gr") чтобы открыть диалог
4. Перейдите на вкладку **"Операции"**
5. Нажмите **"Добавить операцию"**

---

### 1. Раскрой баннера ( Cutting )

#### Настройки операции:

**Основные поля:**
- **Название операции:** `Раскрой баннера` (или `Раскрой с подворотом`)
- **Описание:** `Раскрой баннера с припуском на подворот (без расчёта площади)`
- **Тип операции:** `CUTTING` (Раскрой)
- **Базовая цена:** `0` (стоимость раскроя уже включена в цену материала)
- **Ед. изм.:** `шт`
- **Коэффициент отходов:** `1` (не применяется)
- **Требует размеры:** ✗ (unchecked)
- **Доп. материалы:** ✗ (unchecked)

**Параметры (добавить 3 параметра):**

| Поле | Значение |
|------|----------|
| `marginWidth` | **Ключ:** `marginWidth` • **Название:** `Припуск по ширине (мм)` • **Тип:** `NUMBER` • **Ед.изм.:** `мм` • **По умолч.:** `50` • **Обязательное:** ✗ |
| `marginHeight` | **Ключ:** `marginHeight` • **Название:** `Припуск по высоте (мм)` • **Тип:** `NUMBER` • **Ед.изм.:** `мм` • **По умолч.:** `50` • **Обязательное:** ✗ |
| `sides` | **Ключ:** `sides` • **Название:** `Количество сторон` • **Тип:** `NUMBER` • **По умолч.:** `1` • **Обязательное:** ✗ |

**Формула для quantityFormula (в шаблоне продукта):**
```
(marginWidth * width * sides + marginHeight * height * sides) / 1_000_000
```

**Примечание:**
- Количество операции (в заказе) указывается вручную: `1` или `2` шт. (на одну сторону)
- Площадь материала рассчитывается отдельно (с учётом припусков) и включает стоимость раскроя.
- Эта операция служит для учёта работ; её стоимость не начисляется отдельно.
- Формула вычисляет площадь подворотов в м² на основе параметров операции. Для полного учёта материала используйте формулу в материале.

---

### 2. Печать баннера (Printing)

#### Настройки операции:

**Основные поля:**
- **Название операции:** `Печать баннера`
- **Описание:** `Печать на баннере`
- **Тип операции:** `PRINT`
- **Базовая цена:** `150` (или ваша ставка за м²)
- **Ед. изм.:** `м²`
- **Коэффициент отходов:** `1.1` (10% отходов)
- **Требует размеры:** ✓ (checked)
- **Доп. материалы:** ✗ (unchecked)

**Параметры (2 параметра):**

| Поле | Значение |
|------|----------|
| `width` | **Ключ:** `width` • **Название:** `Ширина печати (мм)` • **Тип:** `NUMBER` • **Ед.изм.:** `мм` • **Обязательное:** ✓ |
| `height` | **Ключ:** `height` • **Название:** `Высота печати (мм)` • **Тип:** `NUMBER` • **Ед.изм.:** `мм` • **Обязательное:** ✓ |

**Формула (если нужна):**
```
width * height / 1_000_000
```

**Пример заполнения в заказе:**
- Ширина: 2000 мм
- Высота: 1000 мм
- **Результат:** `2.0000 м²`

**Примечание:** Припуск на печать не добавляется — используйте чистые размеры макета.

---

### 3. Ламинирование/оклейка (Lamination)

#### Настройки операции:

**Основные поля:**
- **Название операции:** `Ламинирование`
- **Описание:** `Ламинирование баннера`
- **Тип операции:** `LAMINATION`
- **Базовая цена:** `80` (ставка за м²)
- **Ед. изм.:** `м²`
- **Коэффициент отходов:** `1.05` (5% отходов)
- **Требует размеры:** ✓ (checked)
- **Доп. материалы:** ✗ (unchecked)

**Параметры (4 параметра):**

| Поле | Значение |
|------|----------|
| `width` | **Ключ:** `width` • **Название:** `Ширина печати (мм)` • **Тип:** `NUMBER` • **Ед.изм.:** `мм` • **Обязательное:** ✓ |
| `height` | **Ключ:** `height` • **Название:** `Высота печати (мм)` • **Тип:** `NUMBER` • **Ед.изм.:** `мм` • **Обязательное:** ✓ |
| `marginWidth` | **Ключ:** `marginWidth` • **Название:** `Припуск по ширине (мм)` • **Тип:** `NUMBER` • **Ед.изм.:** `мм` • **По умолч.:** `20` • **Обязательное:** ✗ |
| `marginHeight` | **Ключ:** `marginHeight` • **Название:** `Припуск по высоте (мм)` • **Тип:** `NUMBER` • **Ед.изм.:** `мм` • **По умолч.:** `20` • **Обязательное:** ✗ |

**Формула (с нахлестом):**
```
(width + marginWidth*2) * (height + marginHeight*2) / 1_000_000
```

**Пример заполнения в заказе:**
- Ширина печати: 2000 мм
- Высота печати: 1000 мм
- Припуск: 20 мм с каждой стороны
- **Результат:** `(2000+40) × (1000+40) / 1_000_000 = 2.1456 м²`

**Варианты:**
- Без нахлеста: оставьте припуски `0`
- Одностороннее ламинирование: коэффициент `1`
- Двустороннее: учтите в формуле или создайте отдельную операцию

---

### 4. Установка люверсов (Eyebolts) — с шагом

#### Настройки операции:

**Основные поля:**
- **Название операции:** `Установка люверсов` (или `Люверсы`)
- **Описание:** `Установка люверсов с шагом`
- **Тип операции:** `EYELETS`
- **Базовая цена:** `15` (цена за один люверс)
- **Ед. изм.:** `шт`
- **Коэффициент отходов:** `1` (не применяется)
- **Требует размеры:** ✓ (checked) — нужны для расчёта количества
- **Доп. материалы:** ✓ (checked) — можно добавить клей, скотч и т.д.

**Параметры (3 параметра):**

| Поле | Значение |
|------|----------|
| `step` | **Ключ:** `step` • **Название:** `Шаг установки (мм)` • **Тип:** `NUMBER` • **Ед.изм.:** `мм` • **По умолч.:** `500` • **Обязательное:** ✗ |
| `edgeDistance` | **Ключ:** `edgeDistance` • **Название:** `Отступ от края (мм)` • **Тип:** `NUMBER` • **Ед.изм.:** `мм` • **По умолч.:** `50` • **Обязательное:** ✗ |
| `diameter` | **Ключ:** `diameter` • **Название:** `Диаметр люверса (мм)` • **Тип:** `NUMBER` • **Ед.изм.:** `мм` • **По умолч.:** `12` • **Обязательное:** ✗ |

**Важно:** Параметры `width` и `height` больше не требуются — они берутся автоматически из размеров изделия (полей позиции заказа). Галочка **"Требует размеры"** должна быть **снята**.

**Формула расчёта количества (JEXL):**
```
helper.eyeletCount(step, edgeDistance, quantity)
```

**Примечание:**
- Ширина (`width`) и высота (`height`) изделия берутся автоматически из полей позиции заказа (как в расчёте раскроя) и не передаются в формулу явно.
- `quantity` — количество изделий в позиции (передаётся автоматически).
- Параметры `step` и `edgeDistance` — из формы операции.
- Диаметр люверса (`diameter`) является справочным параметром и не участвует в расчёте количества.

**Алгоритм (backend):**
```java
// Конвертируем в мм (width и height из позиции заказа)
wmm = width * 1000
hmm = height * 1000

// Эффективные размеры с учётом отступа
effW = wmm - 2 * edgeDistance
effH = hmm - 2 * edgeDistance

// Количество точек по каждой стороне (округление вверх)
countW = ceil(effW / step)
countH = ceil(effH / step)

// Всего люверсов: 2 по ширине + 2 по высоте, минимум 4
totalPerBanner = max(4, 2*countW + 2*countH)

// Общее количество = на один баннер × количество баннеров (quantity)
total = totalPerBanner * quantity
```

**Пример заполнения в заказе:**
- Ширина: 2000 мм
- Высота: 1000 мм
- Шаг: 500 мм
- Отступ от края: 50 мм
- Количество баннеров: 2
- **Результат:** `4 люверса на баннер × 2 шт. = 8 шт.`

**Примечание:** Если шаг не задан — используется 500 мм по умолчанию.

---

### 5. Сварка/пошив каркаса (Frame Welding)

#### Настройки операции:

**Основные поля:**
- **Название операции:** `Сварка каркаса` (или `Сварка обрамления`)
- **Описание:** `Сварка каркаса из проф. трубы`
- **Тип операции:** `WELDING`
- **Базовая цена:** `150` (цена за пог.м)
- **Ед. изм.:** `пог.м` (или `м.п.`)
- **Коэффициент отходов:** `1`
- **Требует размеры:** ✓ (checked)
- **Доп. материалы:** ✗ (unchecked)

**Параметры (2 параметра):**

| Поле | Значение |
|------|----------|
| `width` | **Ключ:** `width` • **Название:** `Ширина изделия (мм)` • **Тип:** `NUMBER` • **Ед.изм.:** `мм` • **Обязательное:** ✓ |
| `height` | **Ключ:** `height` • **Название:** `Высота изделия (мм)` • **Тип:** `NUMBER` • **Ед.изм.:** `мм` • **Обязательное:** ✓ |

**Формула (Hansen):**
```
2 * (width + height) / 1000
```

**Пример заполнения в заказе:**
- Ширина: 2000 мм
- Высота: 1000 мм
- **Результат:** `2 × (2.0 + 1.0) = 6.0 пог.м`

---

### 6. Монтаж конструкции (Installation)

#### Настройки операции:

**Основные поля:**
- **Название операции:** `Монтаж баннера`
- **Описание:** `Монтаж баннера на конструкцию`
- **Тип операции:** `INSTALLATION`
- **Базовая цена:** `2500` (фиксированная цена за монтаж)
- **Ед. изм.:** `шт`
- **Коэффициент отходов:** `1`
- **Требует размеры:** ✗ (unchecked)
- **Доп. материалы:** ✗ (unchecked)

**Параметры (2 параметра — опционально):**

| Поле | Значение |
|------|----------|
| `mountType` | **Ключ:** `mountType` • **Название:** `Тип монтажа` • **Тип:** `SELECT` • **Варианты:** `настенный`, `кронштейн`, `creep` • **По умолч.:** `настенный` |
| `height` | **Ключ:** `height` • **Название:** `Высота установки (мм)` • **Тип:** `NUMBER` • **Ед.изм.:** `мм` • **По умолч.:** `3000` |

**Логика:** Обычно 1 шт. на одну конструкцию. Цена фиксированная, но может зависеть от высоты (учитывается в формуле в продукте).

**Пример заполнения в заказе:**
- Количество: 1
- Тип монтажа: `настенный`
- Высота установки: 3000 мм
- **Результат:** `1 × 2500 ₽ = 2500 ₽`

---

## Общие принципы заполнения в форме заказа (CreateOrderForm)

### Когда создаёте заказ:
1. Выбираете материал (например, баннер)
2. Вводите размеры **в миллиметрах** (Ширина × Высота)
3. Нажимаете **"Операция"** → выбираете нужную операцию
4. В форме операции заполняете параметры:
   - Поля с `Обязательное: ✓` — должны быть заполнены
   - Поля с `По умолч.` — можно оставить пустыми (подставятся значения по умолчанию)
5. Видите **предрасчёт** количества внизу формы
6. Нажимаете **"Добавить"** → операция добавляется к позиции
7. При необходимости добавляете несколько операций

### Калькуляция итога:
Материалы и операции считаются отдельно:
```
Итого по позиции = 
  (Материал: цена × кол-во × коэф.отходов) + 
  (Операция: цена × кол-во × коэф.отходов)
```

---

## Формулы для Product Templates

Если создаёте **шаблон продукта** (Product), можно предзаполнить операции с формулами.

### Пример шаблона "Баннер 2x1 м":

```json
{
  "name": "Баннер 2×1 м",
  "width": 2000,
  "height": 1000,
  "materials": [
    {
      "materialId": 21,
      "quantityFormula": "(width + 50*2) * (height + 50*2) / 1_000_000",
      "wasteCoefficient": 1.1
    }
  ],
  "operations": [
    {
      "name": "Печать баннера",
      "pricePerUnit": 150,
      "unit": "м²"
    },
    {
      "name": "Раскрой баннера",
      "pricePerUnit": 0,
      "unit": "шт"
    },
    {
      "name": "Ламинирование",
      "pricePerUnit": 80,
      "unit": "м²"
    },
     {
       "name": "Установка люверсов",
       "pricePerUnit": 15,
       "unit": "шт",
       "quantityFormula": "helper.eyeletCount(step, edgeDistance, quantity)"
     }
  ]
}
```

---

## Валидация полей

| Поле | Ограничения |
|------|-------------|
| `width`, `height` | > 0 |
| `marginWidth`, `marginHeight` | ≥ 0 |
| `step` | > 0 (мин. 50) |
| `edgeDistance` | 30–50 мм |
| `diameter` | 8, 12 или 16 мм |
| `quantity` | > 0 |

---

## Важные замечания

1. **Единицы измерения:** все размеры вводятся в **миллиметрах**, но в БД хранятся в метрах (делим на 1000 или 1_000_000)
2. **Припуски:** учитывайте их в операции **раскроя**, не в печати
3. **Коэффициент отходов:** применяется к материалу (не к операции)
4. **Тип операции:** выбирайте из справочника (PRINT, CUTTING, LAMINATION, WELDING, EYELETS, INSTALLATION…)
5. **Формулы в JEXL:** можно использовать `width`, `height`, `helper.eyeletCount()`, арифметику `+−*/`, `ceil()`, `max()`, `min()`

---

## Быстрая настройка в админке (cheatsheet)

Зайдите в админку →Materials→ Выберите материал →Операции→Добавить

**Раскрой:**
```
Название: Раскрой баннера
Тип: CUTTING
Цена: 0  (включена в стоимость материала)
Ед.изм.: шт
Требует размеры: ✗

Параметры:
  marginWidth  — Припуск по ширине (мм)   NUMBER, мм, дефолт 50
  marginHeight — Припуск по высоте (мм)   NUMBER, мм, дефолт 50
  sides        — Количество сторон         NUMBER,      дефолт 1
```

**Печать:**
```
Название: Печать баннера
Тип: PRINT
Цена: 150
Ед.изм.: м²
Требует размеры: ✓

Параметры:
  width  — Ширина печати (мм)  NUMBER, мм, обязательное ✓
  height — Высота печати (мм)  NUMBER, мм, обязательное ✓
```

**Люверсы (с шагом):**
```
Название: Установка люверсов
Тип: EYELETS
Цена: 15
Ед.изм.: шт
Требует размеры: ✗  (снять галочку — размеры берутся из позиции заказа)

Параметры:
  step          — Шаг установки (мм)      NUMBER, мм,    дефолт 500
  edgeDistance  — Отступ от края (мм)     NUMBER, мм,    дефолт 50
  diameter      — Диаметр люверса (мм)    NUMBER, мм,    дефолт 12
```

---

*Документ обновлён с инструкциями по настройке операций в админке. Все формулы JEXL работают через движок CalculatorService.*